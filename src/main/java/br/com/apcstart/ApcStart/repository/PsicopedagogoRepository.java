package br.com.apcstart.ApcStart.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import br.com.apcstart.ApcStart.model.Psicopedagogo;

public interface PsicopedagogoRepository extends JpaRepository<Psicopedagogo, Integer> {
  
}
